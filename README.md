# Count Down Timer OBS
 Sets a timer for as many minutes as you input and outputs it to a file for obs to read

 Counts down for x minutes writing to a file for obs to read and display using a text source